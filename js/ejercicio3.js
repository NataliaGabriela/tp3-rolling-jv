const max = 6;
const min = 1;
const arraySuma = [];

for (let i = 0; i < 50; i++) {
  
    let sumaDeAleatorios = 0;
    for (let lanzamientos = 0; lanzamientos < 2; lanzamientos++) {
      sumaDeAleatorios += Math.floor(Math.random() * (max - min + 1) + min);
    }
    arraySuma[i];
  
  }


